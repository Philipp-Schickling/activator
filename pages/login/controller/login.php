<?php

	// print_r( $_SESSION );

	// // create salt for hidden input field in login/register form
	// $_SESSION["salt"] = getSalt();

	// $salt = getSalt();

	// $_ = new URL;
	// $pageTitle = $_ -> getTitle( $_GET );

	// $secondArgument = $_ -> getSecondArgument( $_GET );

?>
